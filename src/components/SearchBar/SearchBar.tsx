import "./SearchBar.scss";

const SearchBar = () => {
  return (
    <div className="search-bar">
      <label>Search: </label>
      <input type="text" />
    </div>
  );
};

export default SearchBar;
